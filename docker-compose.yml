version: '3.9'

services:
  backend:
    build:
      context: ./
      dockerfile: ./docker/Dockerfile
      target: production
    environment:
      - DJANGO_SETTINGS_MODULE=FactFlow.settings.production
      - PYTHONUNBUFFERED=1
    ports:
      - "8000:8000"

